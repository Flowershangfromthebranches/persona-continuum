# Roadmap

- Add richer MCP in-memory transport tests.
- Add source-level deletion invalidation across derived claims and memories.
- Add optional local embedding adapters without cloud API dependency.
- Add OCR, audio transcription, and video understanding extension interfaces.
- Add larger multi-agent continuation backends behind the current branch API.
- Package the Skill and MCP config as an installable Codex plugin.
